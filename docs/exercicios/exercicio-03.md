# Exercícios - Aula 03 📝

### 🟢 Básicos
1. Qual a diferença entre os modificadores `public` e `private`?
2. Para que serve um método construtor em Java?

### 🟡 Intermediários
3. Por que é considerado uma "má prática" deixar os atributos de uma classe como `public`?
4. Explique o uso da palavra-chave `this` dentro de um método ou construtor.

### 🔴 Desafio
5. Crie uma classe `Conta` com o atributo `saldo` privado. Implemente um método `sacar(double valor)` que verifique se o saldo é suficiente antes de realizar a operação. Se não for, exiba uma mensagem de erro.
