# Exercícios - Aula 06 📝

### 🟢 Básicos
1. Qual a regra de ouro para nomear pacotes (packages) em Java?
2. O que acontece se tivermos duas classes com o mesmo nome em pacotes diferentes?

### 🟡 Intermediários
3. Explique a importância da separação de responsabilidades (camadas).
4. O que o comando `import` faz no topo do arquivo Java?

### 🔴 Desafio
5. Desenhe a estrutura de pastas de um projeto chamado "SistEscola" que contenha as camadas de `view`, `model` e `controller`.
