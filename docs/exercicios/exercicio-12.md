# Exercícios - Aula 12 📝

### 🟢 Básicos
1. Qual a diferença entre uma "Tabela" e um "Registro" em um banco de dados?
2. Para que serve o comando `SELECT` em SQL?

### 🟡 Intermediários
3. Escreva o comando SQL para criar uma tabela chamada `cliente` com as colunas `id` (chave primária), `nome` (texto) e `email` (texto).
4. Explique por que não é recomendado guardar senhas de usuários em texto limpo (sem criptografia) no banco de dados.

### 🔴 Desafio
5. Modele um banco de dados para uma "Livraria" com duas tabelas: `Livro` e `Autor`. Como você faria para ligar um livro a um autor?
