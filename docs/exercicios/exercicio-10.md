# Exercícios - Aula 10 📝

### 🟢 Básicos
1. Qual componente você usaria para uma pergunta de "Sim/Não" independente?
2. Por que usar `Layout Managers` em vez de coordenadas fixas (X, Y)?

### 🟡 Intermediários
3. Descreva a diferença entre `FlowLayout` e `GridLayout`.
4. Como você garantiria que apenas um dos 4 `JRadioButtons` de uma pergunta fosse selecionado?

### 🔴 Desafio
5. Projete a estrutura de uma calculadora simples (0-9, +, -, =) usando `BorderLayout` para o visor e `GridLayout` para os botões.
