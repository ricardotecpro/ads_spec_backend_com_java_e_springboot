# Exercícios - Aula 02 📝

### 🟢 Básicos
1. Qual a diferença prática entre uma **Classe** e um **Objeto**? Use uma metáfora diferente da "planta da casa".
2. No código Java, qual a palavra-chave utilizada para criar uma instância de um objeto?

### 🟡 Intermediários
3. Se você criar uma classe `Livro` com os atributos `titulo` e `autor`, escreva como você instanciaria dois livros diferentes.
4. O que acontece na memória do computador quando executamos o comando `new MinhaClasse()`?

### 🔴 Desafio
5. Modele uma classe `Computador` com 4 atributos (ex: marca, ram) e 2 métodos (ex: ligar, desligar). Represente isso através de um diagrama Mermaid `classDiagram`.
