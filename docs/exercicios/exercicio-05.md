# Exercícios - Aula 05 📝

### 🟢 Básicos
1. Qual a principal diferença entre uma Classe Comum e uma Classe Abstrata?
2. Como se chama op processo de uma classe "cumprir" os métodos definidos em uma Interface?

### 🟡 Intermediários
3. Por que dizemos que uma Interface é um "contrato"?
4. Uma classe pode herdar de uma classe abstrata e implementar duas interfaces ao mesmo tempo? Explique.

### 🔴 Desafio
5. Projete a interface `Autenticavel` com o método `autenticar(String login, String senha)`. Crie a classe `Usuario` que implementa essa interface.
