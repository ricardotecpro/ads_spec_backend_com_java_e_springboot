# Exercícios - Aula 08 📝

### 🟢 Básicos
1. Qual a função principal do `JFrame` em uma aplicação Swing?
2. Cite 3 componentes visuais que podem ser adicionados a uma janela.

### 🟡 Intermediários
3. Explique por que a programação de interface gráfica é chamada de "Orientada a Eventos".
4. O que acontece com o processo do Java quando fechamos uma janela (`JFrame`) se não configurarmos o `setDefaultCloseOperation`?

### 🔴 Desafio
5. Desenhe (ou descreva) o ciclo de vida de um clique em um botão: desde a ação física do usuário até a execução do método no Java.
