# Projeto 02 - Modelagem de Entidades 🧱

Vamos aplicar os conceitos de Classes e Objetos para modelar produtos de um sistema de estoque.

## Atividades
1. Criar a classe `Produto` com os atributos `nome`, `preco` e `quantidade`.
2. Implementar um método `exibirInfo()` que mostre os dados no console.
3. No método `main`, instanciar pelo menos 3 produtos diferentes (ex: Teclado, Mouse, Monitor).
4. Atualizar o preço de um dos produtos e exibir os dados novamente.

## Entrega
- Código da classe `Produto`.
- Código da classe `Main` para teste.
