# Projeto 12 - Criando a Base de Dados 🗄️

Modelagem e criação física do banco de dados relacional.

## Atividades
1. Instalar o SQLite Browser (ou usar terminal).
2. Criar o arquivo `meusistema.db`.
3. Executar o comando SQL para criar a tabela `produto` com ID auto-incremento.
4. Inserir manualmente 5 registros de teste no banco.

## Entrega
- Print do SQLite Browser exibindo a tabela e os dados.
- O arquivo do banco de dados (`.db`).
