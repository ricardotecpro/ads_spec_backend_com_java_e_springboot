# Projeto 13 - Fábrica de Conexões 🔌

Implementação da ponte segura entre o Java e o SQLite.

## Atividades
1. Criar a classe `ConexaoDB` usando o padrão Singleton ou apenas métodos estáticos.
2. Implementar o método `getConexao()` que retorna uma `java.sql.Connection`.
3. Tratar as exceções de banco de dados com `try-catch`.
4. Criar um método `main` simples apenas para testar se a conexão abre e fecha sem erros.

## Entrega
- Código da classe de conexão.
- Console exibindo "Conectado com sucesso!".
